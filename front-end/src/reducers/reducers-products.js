import { FETCH_PRODUCTS } from "../actions";

const ProductReducer = function (state = [], action) {
  switch (action.type) {
    case FETCH_PRODUCTS:
      
      break;
  
    default:
      return state;
  }
}