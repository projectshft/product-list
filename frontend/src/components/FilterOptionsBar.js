import React from "react";
import Typography from "@material-ui/core/Typography";

class FilterOptionsBar extends React.Component {
  render() {
    return (
      <div>
        <Typography variant="h2">FilterOptionsBar</Typography>
      </div>
    );
  }
}

export default FilterOptionsBar;
