import { useSelector } from 'react-redux';
import './Controls.css';

const Controls = () => {
    return (
        <div className="ProductList">
            <h1>Controls</h1>
            
        </div>
    )
};

export default Controls;